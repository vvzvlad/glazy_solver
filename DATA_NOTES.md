# Данные проекта: материалы, глазури, оксиды

Разбор происхождения `database/` и найденные в нём проблемы. Документ написан
по результатам исследования SegerLab (segerlab.ru) и сверки его данных с нашими.
Все числа ниже воспроизведены на текущем коде репозитория, не взяты на веру.

Сырая выгрузка из SegerLab лежит в `/tmp/segerlab/` (вне репозитория):
`materials.json`, `glazes.json`, `oxide_roles.json`, `api-notes.md`.

---

## 0. Кратко: что делать

1. **Починить классификацию оксидов** — сейчас `MnO2` не входит ни в одну группу,
   из-за чего UMF марганцевых рецептов раздувается в ~117 раз (раздел 2).
2. **Решить вопрос про `Fe2O3`** — флюс он или стабилизатор. От этого зависит
   систематическое расхождение всех наших UMF с SegerLab на 0.2% (раздел 3).
3. **Переписать комментарий к пропущенному тесту** `test_recipe_10` — он
   диагностирует причину неверно (раздел 2.4).
4. **Забрать флаг `isWaterSoluble`** — он есть у SegerLab, у нас потерян при
   выгрузке, а для решателя он значим (раздел 1.3).
5. **Не считать 11 эталонных рецептов внешним оракулом** — они из того же
   источника, что и наши материалы (раздел 4).

---

## 1. Происхождение базы

### 1.1. `database/materials.json` — это дамп публичной библиотеки SegerLab

Доказано сверкой:

| Признак | Наш файл | SegerLab |
| --- | --- | --- |
| Число записей | 216 | 216 |
| Поля записи | `id`, `name`, `description`, `formula`, `isPublic`, `inInventory`, `userId` | те же + `isWaterSoluble` |
| Нефелин-сиенит VR13 | SiO2 62.93, Al2O3 20.33, K2O 7.57, Na2O 7.09 | идентично |
| Каолин КЖФ-1 | SiO2 47, Al2O3 36, K2O 1, Fe2O3 0.6, TiO2 0.4, CaO 0.15, Na2O 0.07 | идентично |
| Улексит (Химпэк) | B2O3 37, CaO 19, SiO2 4, Na2O 3.5, MgO 2.5, SrO 1, Al2O3 0.25, Fe2O3 0.04 | идентично |

Совпадают и `id`: `Нефелин-сиенит VR13` = 216, `Каолин КЖФ-1` = 41,
`Волластонит МИВОЛЛ` = 54, `Улексит (Химпэк)` = 10, `Кварцевая мука Кварцверке W12` = 36.

`userId` во всех записях — служебный аккаунт-владелец общей библиотеки SegerLab.

### 1.2. `database/recipes.json` — это их 11 шаблонов «Основы OVO △6»

Совпадают имена, порядок, состав базы, состав добавок и флаг
`includeAdditionsIntoCalculations`. Поле `type` в наших записях
(«Основы OVO △6») — это их `templateGroup`.

Дефолтный UMF в текущем UI (`UI/js/app.js`, `current_umf`) — это формула
их «Прозрачной глазури △6».

### 1.3. Чего в нашей выгрузке не хватает

- **`isWaterSoluble`** — булев флаг, у SegerLab им помечены 27 материалов из 216
  (нефелин, бораты, сода, поташ и др.). Для решателя значим: водорастворимый
  компонент нельзя подставлять свободно, он уходит в раствор при замесе.
  Стоит перенести флаг в `materials.json` и учитывать хотя бы предупреждением.
- **`Loi`** — у них опциональный ключ, заполнен всего у 3 материалов из 216,
  так что переносить нечего. Важно другое: **ППП у них не моделируется вообще**.
  Анализы вводятся «как в паспорте», сумма обычно меньше 100 (медиана ≈ 98.5,
  у глин 85–91), и потери при прокаливании остаются неявным «остатком до 100».
  Наш `calculate_recipe_composition` работает с той же конвенцией, поэтому
  расчёт согласован, но это надо знать: **ППП в наших данных нигде не задан явно**,
  он только выводится как `100 − сумма оксидов`.
- **37 материалов с пустой `formula`** (пигменты, SiC всех фракций, КМЦ, вода,
  гипс) — это не битые записи, а сознательное решение источника: такие
  компоненты живут в рецепте, но в химию не входят. Ступень 1.4 ТЗ требует
  исключать материалы с нулевой суммой формулы из матрицы NNLS — это правильно,
  но в валидации базы (ступень 2.3) их **нельзя** помечать как `error`, иначе
  валидатор будет ругаться на 37 легальных записей.

---

## 2. Классификация оксидов — здесь баг

### 2.1. Как классифицирует SegerLab

Их роли (`GET /api/RecipeCalculation/oxide-roles`): `Alcali` (R2O),
`AEarth` (RO), `Stabs` (R2O3), `GFormers` (RO2), `Other`.
Unity = `Alcali + AEarth = 1`. Переключения конвенций у них нет.

Расхождения с нашей `common.oxides_classification()`:

| Оксид | У нас | У SegerLab | Флюс у нас? | Флюс у них? |
| --- | --- | --- | --- | --- |
| `Fe2O3` | r2o3 | **AEarth** | нет | **да** |
| `MnO2` | **нигде** | **AEarth** | **нет** | **да** |
| `TiO2` | ro2 | Stabs | нет | нет |
| `NiO` | ro | Stabs | да | нет |
| `CuO`, `Cu2O` | ro / — | **Alcali** | да / — | да |
| `SnO2` | ro2 | **Alcali** | нет | **да** |
| `P2O5` | — | GFormers | нет | нет |
| `V2O5`, `CdO` | — | AEarth | нет | да |
| `ZrO2`, `CeO2`, `GeO2` | ro2 / — | GFormers | нет | нет |

Значение имеет только колонка «флюс»: она задаёт знаменатель unity-нормировки.
Различия внутри не-флюсовых групп на числа не влияют, только на то, в какой
колонке оксид показан.

### 2.2. Главная проблема: `MnO2` не классифицирован вообще

`common.py:19-25` перечисляет `MnO` в `ro` и `Mn2O3` в `r2o3`, но **`MnO2`
не входит ни в одну группу**. При этом два материала базы несут именно `MnO2`:

- `Оксид марганца` — `{MnO2: 86.9}`
- `Карбонат марганца` — `{MnO2: 61.62}`

Следствие для рецепта «Марганцевый металлик △6»
(Оксид марганца 50 / Каолин КЖФ-1 30 / Кварцевая мука 20):

| Оксид | Наша фикстура | SegerLab |
| --- | --- | --- |
| SiO2 | **131.188** | 1.123 |
| MnO2 | **115.529** | 0.989 |
| Al2O3 | **24.484** | 0.210 |
| K2O | 0.736 | 0.006 |
| TiO2 | 0.347 | 0.003 |

Раздув ровно в ~117 раз. Причина арифметическая: в этом рецепте почти вся
масса — марганец, и если `MnO2` не флюс, то сумма флюсов схлопывается до
≈ 0.0086 (только следовые K2O и CaO из каолина), а деление на неё разносит
всю формулу.

### 2.3. Ступень 1 ТЗ этот баг не чинит

`TZ_SOLVER_V2.md`, раздел 1.2, предлагает
`"ro2": ["SiO2", "TiO2", "ZrO2", "SnO2", "GeO2", "MnO2"]`, а `unity` оставляет
как `["r2o", "ro"]`. То есть `MnO2` перестаёт быть неклассифицированным, но
флюсом не становится — числа останутся такими же неадекватными.

**Рекомендация:** положить `MnO2` в `ro`. Это согласуется и с SegerLab
(`AEarth`), и с калькулятором Лаборатории керамики, где двухвалентные оксиды
красителей входят в единицу щелочей «потому что они так и работают».

### 2.4. Комментарий к пропущенному тесту неверен

`tests/test_individual_recipes.py:320-330` объясняет пропуск так:

> no material in `database/materials.json` carries MnO2 at all, so nothing in
> the inventory can supply it

Первая половина фактически неверна: `MnO2` несут два материала (см. 2.2).
Верно только то, что ни один из них не помечен `inInventory: true`.
Но настоящая причина недостижимости другая — не отсутствие сырья, а
целевое значение `MnO2 ~115`, которое само по себе является артефактом
неклассифицированного оксида. После починки классификации цель станет
`MnO2 ≈ 0.99`, и рецепт станет решаемым, как только источник марганца
попадёт в инвентарь.

---

## 3. Систематическое расхождение 0.2% и его причина

На всех рецептах без марганца наши числа больше SegerLab ровно в 1.0022 раза:

| Рецепт | Наш SiO2 | SegerLab SiO2 |
| --- | --- | --- |
| Прозрачная △6 | 3.151 | 3.144 |
| Матовая кальциевая △6 | 2.455 | 2.448 |
| Магниевая матовая △6 | 2.120 | 2.112 |
| Флотинг △6 | 3.461 | 3.452 |
| Глуп △6 | 5.532 | 5.501 |

Причина найдена и проверена на «Прозрачной △6». Их `AEarth` для этого рецепта:

```
CaO 0.717 · MgO 0.048 · SrO 0.005 · Fe2O3 0.002
```

**`Fe2O3` входит у них в знаменатель unity, у нас — нет.** Наша сумма флюсов
равна 1.000, их — 1.002. Отсюда `3.151 / 1.002 = 3.1447 ≈ 3.144`, и так же
для всех остальных оксидов (`0.718 → 0.717`, `0.379 → 0.378`, `0.266 → 0.265`).

То есть всё расхождение — одно решение о роли `Fe2O3`, а не накопленная
погрешность и не разные молярные массы.

**Это решение надо принять сознательно.** Классический UMF держит `Fe2O3`
в R2O3 (наш вариант); SegerLab и «расширенные» конвенции считают его флюсом.
Ступень 7.2(а) ТЗ уже предусматривает пресеты `unity_presets` — сюда просится
третий пресет, `segerlab`, чтобы можно было сверяться с их числами один в один:

```json
"segerlab": ["Na2O", "K2O", "Li2O", "CuO", "Cu2O", "SnO2",
             "MgO", "CaO", "SrO", "BaO", "ZnO", "PbO", "CdO",
             "MnO", "MnO2", "FeO", "Fe2O3", "CoO", "V2O5"]
```

---

## 4. Эталонные глазури

11 шаблонов «Основы OVO △6», все на отечественном сырье, база нормирована
ровно к 100, добавки сверх базы, `includeAdditionsIntoCalculations: false`
у всех одиннадцати.

Формула Зегера от SegerLab (их числа, их конвенция):

| Шаблон | Ключевые значения UMF |
| --- | --- |
| Прозрачная △6 | R2O 0.229 / RO 0.771 · CaO 0.717 · Al2O3 0.378 · B2O3 0.265 · SiO2 3.144 · Si:Al 8.32 |
| Матовая кальциевая △6 | CaO 0.704 · Al2O3 0.496 · B2O3 0.156 · SiO2 2.448 · Si:Al 4.9 |
| Магниевая матовая △6 | MgO 0.428 · CaO 0.303 · ZnO 0.132 · Al2O3 0.194 · SiO2 2.112 · Si:Al 10.9 |
| Матовая недожога △6 | CaO 0.870 · Al2O3 0.207 · SiO2 1.575 |
| Флотинг △6 | CaO 0.542 · MgO 0.192 · B2O3 0.384 · Al2O3 0.355 · SiO2 3.452 |
| Сборка △6 | ZnO 0.707 · Al2O3 0.541 · SiO2 2.308 · Si:Al 4.3 |
| Цинковая кристаллическая △6 | ZnO 0.594 · CaO 0.242 · Al2O3 0.169 · TiO2 0.151 · SiO2 2.092 · Si:Al 12.4 |
| Пенная △6 | CaO 0.779 · Al2O3 0.436 · SiO2 2.659 |
| Белая глянцевая △6 | CaO 0.469 · ZnO 0.183 · MgO 0.144 · B2O3 0.158 · SiO2 2.692 |
| Марганцевый металлик △6 | MnO2 0.989 · Al2O3 0.210 · SiO2 1.123 |
| Глуп △6 | CaO 0.736 · Al2O3 0.708 · SiO2 5.501 |

### 4.1. Это не внешний оракул

Ступень 7 ТЗ ищет независимые оракулы для обратной задачи. Эти 11 рецептов
таковыми **не являются по данным**: и материалы, и рецепты у нас из того же
источника. Независим только сам расчёт — их UMF считает .NET-модуль в
WebAssembly, наш считает `common.weights_to_umf`.

Поэтому корректная формулировка: это **оракул нашей UMF-математики и нашей
конвенции unity**, но не проверка достоверности анализов материалов.
Leach 4321 и задача Arbuckle из ступени 7.4 остаются единственными настоящими
внешними оракулами, потому что там материалы заданы числами прямо в фикстуре.

### 4.2. Что с этим сделать полезного

Добавить регрессионный тест «наш forward против SegerLab» на всех 11 рецептах
с допуском 0.005 при включённом пресете `segerlab`. Он ловит ровно два класса
ошибок: расползание конвенции unity и опечатки в молярных массах. Сейчас,
до починки `MnO2`, десять из одиннадцати пройдут, а «Марганцевый металлик»
упадёт — и это правильное поведение теста.

---

## 5. Модель данных SegerLab, если понадобится сверяться

Материал — ровно 8 полей, `formula` в **весовых процентах**:

```json
{"id": 41, "name": "Каолин КЖФ-1", "description": "",
 "formula": {"SiO2": 47, "Al2O3": 36, "K2O": 1, "Fe2O3": 0.6,
             "TiO2": 0.4, "CaO": 0.15, "Na2O": 0.07},
 "isPublic": true, "isWaterSoluble": false, "inInventory": false,
 "userId": "<uuid>"}
```

Инвентарь у них — только булев флаг: ни количеств, ни партий, ни цен,
ни поставщика. Производитель зашит в имя материала
(`Улексит (Химпэк)`, `Волластонит МИВОЛЛ`, `Фритта 100 (Рускерамика)`).

Рецепт — база и добавки раздельными списками, у позиции `isAddition`,
у рецепта `includeAdditionsIntoCalculations`. Это ровно та модель, которую
описывает ступень 6 ТЗ, так что реализовывать её можно один в один.

---

## 6. Чеклист задач

- [ ] `common.oxides_classification()`: добавить `MnO2` в `ro`; заодно
      `PbO`, которого сейчас нет вовсе (ступень 1.2 ТЗ это уже требует).
- [ ] Вынести классификацию в `database/oxide_classification.json`
      (ступень 1.2), но с исправленным `MnO2`, не как в тексте ТЗ.
- [ ] Добавить пресет `segerlab` в `unity_presets` для сверки.
- [ ] Перегенерировать `tests/fixtures/reference_recipes.json` после починки
      классификации — числа «Марганцевого металлика» изменятся радикально.
- [ ] Переписать комментарий и условие пропуска в
      `tests/test_individual_recipes.py:320-330`; после починки рецепт
      становится решаемым при добавлении источника марганца в инвентарь.
- [ ] Перенести `isWaterSoluble` в `database/materials.json` из
      `/tmp/segerlab/materials.json`.
- [ ] В валидации базы (ступень 2.3) не считать `error` материалы с пустой
      формулой — их 37 и они легальны; достаточно `warning` и исключения
      из матрицы решателя.
- [ ] Новый тест: forward-расчёт против 11 формул SegerLab с пресетом
      `segerlab`, допуск 0.005.
