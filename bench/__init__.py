# Benchmark package: the Glazy corpus loader, the quality baseline snapshot and
# the regression diff. Nothing here is imported by the runtime - only by
# tests/test_inverse_corpus.py and by the scripts of this directory.
